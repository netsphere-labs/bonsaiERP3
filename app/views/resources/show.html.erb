
<% @page_title = "Resource: " + @resource.name %>

<%= link_to 'Back', resources_path, :class => 'list' %>

<% content_for :links do %>
  <%= link_to 'Edit...', edit_resource_path(@resource), class: 'btn' %>
<% end %>

