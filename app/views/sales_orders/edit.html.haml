
:ruby
  @page_title = "Edit Sales Order"
  @crumbs = [
    ['Back', sales_order_path(@order.model_obj)] ]


= bootstrap_form_with model: @order, url:{action:"update", id:@order.model_obj}, method: :put, layout: :horizontal do |f|
  = render 'form', f:f
