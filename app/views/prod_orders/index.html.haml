
- @page_title = "Production Order"

- content_for :links do
  %div.btn-group{role:"group"}
    =link_to "New Prod. Order...", new_prod_order_path, class: 'btn'


= paginate(@orders)

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Activo
      %th Fecha de inicio
      %th Fecha de finalización
      %th
  
  %tbody
    - @orders.each do |project|
      %tr
        %td= link_to(project.name, project, :class => 'b')
        %td
          %i(class="#{true_false(project.active)}")
        %td
          = lo(project.date_start)
        %td
          = lo(project.date_end)
        %td
          = link_to icon_edit, edit_project_path(project.id), class: 'dark'
          = link_to icon_delete, project, class: 'dark', method: 'delete', confirm: 'Esta segur@ de eliminar el proyecto?'
