
<% @page_title = "Chart of Accounts" %>

<% content_for :links do %>
  <div class="btn-group" role="group">
    <%= link_to 'New Account...', new_account_path, :class => 'new' %>
  </div>
<% end %>




  
<table class="table">
  <tr>
    <th>name</th>
    <th>active?
    <th>subtype
    <th>currency
    <th>description
  </tr>

<% @accounts.each do |account| %>
  <tr>
    <td><%= account.name %>
    <td><%= account.active  %>
    <td><%= account.subtype %>
    <td><%= account.currency ? (Money::Currency.find(account.currency) || "internal error") : ""  %>
    <td><%= account.description  %>
    <td><%= link_to account.name, account %>
  </tr>
<% end %>
</table>

