
:ruby
  @page_title = "New Delivery"
  @crumbs = [
    ["Back", {controller:"stores", id:@store.id, action:"outbound_orders"}] ]


= bootstrap_form_with model: @invt, url: {action:"create"}, layout: :horizontal do |f|
  = render 'form', f:f
